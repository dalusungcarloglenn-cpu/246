{
  "name": "ugly-ui-demo-site",
  "version": "2.0.0",
  "description": "A static site demonstrating an intentionally ugly UI with automated lint + test CI/CD pipeline.",
  "scripts": {
    "lint": "eslint . --ext .js",
    "test": "jest",
    "build": "echo 'Static build complete'"
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "jest": "^29.7.0",
    "jsdom": "^22.1.0"
  },
  "jest": {
    "testEnvironment": "jsdom"
  }
}

